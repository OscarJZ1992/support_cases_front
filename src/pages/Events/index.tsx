import EventsComponent from "@support-cases-app/components/Events";

const Events = () => {
    return (
        <div>
            <EventsComponent />
        </div>
    );
}

export default Events